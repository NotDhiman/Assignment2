// spa it is a single page application 
// it means the page will have only one html file and the content will be rendered by javascript
// vite
// it is a build tool that allows us to develop and build our application quickly and efficiently
// one way bindind
// it means the data flows in one direction only from the parent component to the child component
// parent componant can pass the state to the child component but the child component cannot change the state of the parent component
// advantage of one way binding is that it makes the application more predictable and easier to debug
// scalable for large applications because it allows us to manage the state of the application in a more organized way
//componants is samll data that can be reused in different parts of the application
// prop is a short for property it is a way to pass data from the parent component to the child component
// state is a way to manage the data of the component it is a local data that can be changed by the component itself
// export default is used to export a component or a function from a file so that it can be imported in another file
// iss mai sirf ek hi component ya function export kar sakte hai
// aagar default export nahi karna hai to named export ka use kar sakte hai jisme hum multiple component ya function export kar sakte hai
// iss mai ham multiple export kar sakte h lekin import karte waqt curly braces ka use karna padta hai
// states
// yeah ek object h jismai data store hota h aur jab bhi state change hota h to component re-render hota h
// it is declred using this.state
// aagar kuch update karna h toh this.setstate use ho gaa

// usestate
// it is a hook that allows us to manage the state of a functional component
